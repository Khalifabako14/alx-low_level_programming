#include "main.h"

/**
 * add - Adds two integers and returns the result.
 * @v: the first number
 * @k: the second number
 * Return: the addition of both
 */

int add(int v, int k)
{
	return (v + k);
}
