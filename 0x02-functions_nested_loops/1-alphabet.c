#include "main.h"

/**
 * print_alphabet - Prints the alpha in lowercase
 * Return: 0(Always)
 */

void print_alphabet(void)
{
	char letter = 'a';

	(while letter <= 'z'; letter++)
	{
	_putchar(letter);
	}

	_putchar('\n');
	return (0);
}
