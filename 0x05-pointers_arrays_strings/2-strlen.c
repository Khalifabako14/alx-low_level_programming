#include "khalifa.h"

/**
 * _strlen - Function that returns the length of a string.
 * @s : s is a character
 * Return: i as a value
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
